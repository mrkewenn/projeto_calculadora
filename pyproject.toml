[project]
name = "projeto-calculadora"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
]

[project.scripts]
calculadora = "calculadora.main:main"

[tool.uv]
package = true